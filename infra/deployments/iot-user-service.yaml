apiVersion: apps/v1
kind: Deployment
metadata:
  name: iot-user-service
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
  selector:
    matchLabels:
      app: iot-user-service
  template:
    metadata:
      labels:
        app: iot-user-service
    spec:
      containers:
      - name: iot-user-service
        image: alexdragnea/disertatie-iot-user-service:latest
        resources:
          limits:
            memory: 1024Mi
            cpu: 1024m
          requests:
            cpu: 768m
            memory: 768Mi
        ports:
        - containerPort: 8002
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8002
          initialDelaySeconds: 30             # Increase delay to give the container more time to be ready
          periodSeconds: 5
          timeoutSeconds: 5
          successThreshold: 1
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8002
          initialDelaySeconds: 30             # Increase delay to give the container more time to be ready
          periodSeconds: 5
          timeoutSeconds: 5
          successThreshold: 1
          failureThreshold: 3